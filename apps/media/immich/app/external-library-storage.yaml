apiVersion: v1
kind: PersistentVolume
metadata:
  name: immich-external-library
  labels:
    app.kubernetes.io/name: immich
    app.kubernetes.io/component: external-library
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 1Ti
  hostPath:
    path: /home/bupd/hdd/data/BUPD_Personal
    type: Directory
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - media-worker
  persistentVolumeReclaimPolicy: Retain
  storageClassName: immich-external-library
  volumeMode: Filesystem
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-external-library
  namespace: immich
  labels:
    app.kubernetes.io/name: immich
    app.kubernetes.io/component: external-library
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Ti
  storageClassName: immich-external-library
  volumeMode: Filesystem
  volumeName: immich-external-library
