apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Flux must be bootstrapped once before it can reconcile the OCI artifact.
# The ref pins the controller and CRD installation to an immutable release.
resources:
  - github.com/fluxcd/flux2/manifests/install?ref=v2.9.2

