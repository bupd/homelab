apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flux
  namespace: flux-system
spec:
  defaultBackend:
    service:
      name: flux-operator
      port:
        number: 9080
  ingressClassName: tailscale
  tls:
    - hosts:
        - flux
